version: 2.1
jobs:
  say-hello:
    docker:
      - image: circleci/node:13.8.0
        steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Heldlo, World!"
  say-world:
    docker:
      -image: circleci/node:13.8.0
    steps:
      - checkout
      - run:
          name: "say world"
          command: "echo hello there, ramon jargo"
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
  say-word-workflow:
    jobs:
      - say-world
